import 'package:flutter/material.dart';

class CrearViaje extends StatelessWidget {
  const CrearViaje({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("CrearViaje");
  }
}